version: '2'

services:
  videar:
    image: mkaichen/videar:latest
    extends:
      file: common-services.yml
      service: videar-setup
    environment:
      - NODE_ENV=production
      - PORT=80
    ports:
      - "80:80"

  videar-test:
    image: mkaichen/videar-test:latest
    extends:
      file: common-services.yml
      service: videar-setup
    environment:
      - NODE_ENV=test
    command: [ "npm", "test" ]
    depends_on: 
      - central_mongodb
      - speech2text

  speech2text:
    image: mkaichen/rocnnet-s2t:latest
    build: 
      context: .
      dockerfile: dockerbuilds/Dockerfile-s2t
    volumes:
      - /usr/src/s2t

  central_mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes: 
      - /data/db
    restart: always
